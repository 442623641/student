export interface UserInfo {
  graduated ? : boolean;
  /*
  用户名称
  */
  name ? : string;

  /*
  角色
  */
  role ? : string;

  /*
  城市
  */
  city ? : string;

  /*
  年级
  */
  grade ? : string;

  /*
   学校
   */
  school ? : string;

  schoolGuid ? : string;

  /*
  学贝
  */
  coin ? : number;

  /*
  年级名称
  */
  gradeName ? : string;

  /*
  城市名称
  */
  cityName ? : string;

  //  /*
  // 令牌
  // */
  //  token: string;

}
